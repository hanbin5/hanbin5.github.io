---
/**
 * DEPRECATED — empty stub. No callers in src/.
 *
 * The archive row is now inlined directly into pages/index.astro and
 * pages/archive.astro so the rendered markup carries the data-tags /
 * data-category attributes the filter script needs.
 *
 * This file is kept (not removed) only because the build sandbox can't
 * unlink it. Run `git rm src/components/ArchiveRow.astro` to drop it.
 */
---
